#include <stdio.h>
int main()
{
    int i = 0;

    // while (i != 0)
    // while (i < 100)
    while (i <= 5)
    {
        printf("%d Allah is my Lord.\n", i);
        i++;
    }

    printf("Now the Value of i is %d", i);

    return 0;
}
