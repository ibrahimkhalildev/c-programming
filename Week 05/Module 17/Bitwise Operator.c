#include <stdio.h>
#include <math.h>

int main()
{
    int a, b, c, d, s;

    // a=-7;
    // c = ~a;

    a = 5;
    b = 8;
    // d = a | b;

    // d = a<<33; // Left Shift
    d = a >> 3; // Right Shift

    printf("%d \n", d);
    return 0;
}
