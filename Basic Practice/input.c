#include <stdio.h>

int main()
{
   //Here we will set a variable and that is "a"
   int a, b;
   printf("Enter two Numbers:");
   //This is Input
   scanf("%d%d",&a, &b);

   printf("The Value of a is %d\n", a);
   printf("The Value of b is %d", b);

   return 0;
}
