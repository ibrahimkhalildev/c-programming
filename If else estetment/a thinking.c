#include <stdio.h>
int main()
{

    int x, a, b, s;
    printf("Enter a number: \n");
    scanf("%d", &x);

    if (x % 2 == 0)
    {
        s = a + b;
    }
    else
    {
        s = a - b;
    }
}