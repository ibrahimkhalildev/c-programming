#include <stdio.h>
int main()
{

    int a = 5, b = 10;
    int sum = a + b;
    int x;

    // printf(" %d \n", c);
    if (a < b)
    {
        printf("A and B are less than 30");
    }
    else
    {
        printf("Function is working!");
    }

    // printf(" %d\n ", sizeof(long long int));
    // printf("%d\n ", sizeof(float));
    // printf("%d\n ", sizeof(double));
    // printf("%d\n ", sizeof(char));

    return 0;
}
